/* ============================================================
   云同步配置 —— 纯静态托管（Cloudflare Pages / GitHub Pages）用
   ------------------------------------------------------------
   使用方法：
   1. 去 https://jsonbin.io 注册免费账号
   2. 创建一个 Bin（内容随便填，比如 {}），复制 Bin ID
   3. 在 API Keys 页面复制 X-Master-Key
   4. 把下面两个「替换我」换成真实值，保存即可
   提示：不配置（保持原样）= 不启用云同步，单机照常使用
   ============================================================ */
window.CLOUD_SYNC = {
  binUrl: 'https://api.jsonbin.io/v3/b/替换我',
  masterKey: '替换我'
};
